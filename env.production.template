# Production Environment Variables for Glumia Solutions
# Copy this file to .env.production and fill in your actual values

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://glumia.com

# SMTP Configuration (Required for contact form and newsletter)
SMTP_USER=your-email@domain.com
SMTP_PASS=your-smtp-password
SMTP_FROM=your-email@domain.com
SMTP_TO=info@glumia.com
SMTP_SUBSCRIPTION_BOX=newsletter@glumia.com

# Optional: Analytics and Monitoring
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# SENTRY_DSN=your-sentry-dsn

# Optional: Database (if you add one later)
# DATABASE_URL=postgresql://user:password@db:5432/glumia

# Security
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=https://glumia.com

# Instructions:
# 1. Copy this file to .env.production
# 2. Replace all placeholder values with your actual credentials
# 3. Ensure SMTP credentials are correct for your email provider
# 4. Generate a secure NEXTAUTH_SECRET (you can use: openssl rand -base64 32)
# 5. Never commit .env.production to version control
